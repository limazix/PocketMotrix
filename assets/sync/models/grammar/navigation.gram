#JSGF V1.0;

grammar navigation;

<global> =  go (home | back | right | left);

<direction> = up | right | down | left;

<active> =  stop listening;

<volume> = quieter | louder | mute;

<action> = enter | <active> | <volume>;

public <command> = <global> | <direction> | <action>;